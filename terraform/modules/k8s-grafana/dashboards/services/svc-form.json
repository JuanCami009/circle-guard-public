{
  "uid": "circleguard-svc-form",
  "title": "CircleGuard — Formularios de Salud (form-service)",
  "description": "Métricas HTTP y encuestas de salud enviadas.",
  "tags": ["circleguard", "per-service"],
  "refresh": "30s",
  "schemaVersion": 38,
  "time": { "from": "now-1h", "to": "now" },
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 1, "title": "HTTP", "type": "row"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 1 },
      "id": 2, "title": "HTTP Request Rate (req/s)",
      "type": "timeseries",
      "targets": [{ "expr": "sum(rate(http_server_requests_seconds_count{application=\"form-service\"}[5m])) by (uri, method)", "legendFormat": "{{method}} {{uri}}", "refId": "A" }]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 1 },
      "id": 3, "title": "HTTP Error Rate (5xx)",
      "type": "timeseries",
      "targets": [{ "expr": "sum(rate(http_server_requests_seconds_count{application=\"form-service\",outcome=\"SERVER_ERROR\"}[5m]))", "legendFormat": "5xx/s", "refId": "A" }]
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 9 },
      "id": 4, "title": "Métricas de Negocio", "type": "row"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 10 },
      "id": 5, "title": "Encuestas Enviadas (por 5m)",
      "type": "timeseries",
      "targets": [{ "expr": "rate(circleguard_surveys_submitted_total[5m])", "legendFormat": "Encuestas/s", "refId": "A" }]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 10 },
      "id": 6, "title": "Encuestas enviadas (24h)",
      "type": "stat",
      "options": { "reduceOptions": { "calcs": ["sum"] } },
      "targets": [{ "expr": "increase(circleguard_surveys_submitted_total[24h])", "legendFormat": "Encuestas hoy", "refId": "A" }]
    }
  ]
}
